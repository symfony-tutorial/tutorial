{% extends 'base.html.twig' %}

{% block body %}

    <h1>Category edit</h1>

    {{ form(edit_form) }}

    <div class="actions">
        <a class="button" href="{{ path('category_list') }}">
            Back to the list
        </a>
    </div>
{% endblock %}
